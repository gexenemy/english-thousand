import React from 'react';

import styled from '@emotion/styled';

const HeaderWrap = styled.h1`
    margin-bottom: 32px;
`;

export const Header = () => {
    return <HeaderWrap>Введите перевод слова</HeaderWrap>;
};
